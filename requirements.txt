prettytable
humanize
confuse
typing_extensions
pyyaml
urllib3>=2.7.0
shtab
# Pinned to 8.x: some APIs we rely on are deprecated in click 9
click>=8,<9

# proxmoxer depends on
proxmoxer>=2.0.1
requests>=2.33.0
requests_toolbelt
openssh_wrapper
paramiko
# Not imported directly, pulled in by paramiko: constrained here because
# paramiko's own floor (>=3.3) allows versions with known advisories
cryptography>=50.0.0
