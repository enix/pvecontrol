# Development dependencies, pinned.
#
# Declarations live in requirements-dev.txt; this file is the resolved closure
# (direct + transitive), used by the dependency graph and for a reproducible
# dev install.
#
# Regenerate with pip-tools after changing requirements-dev.txt:
#   pip install pip-tools
#   pip-compile --strip-extras --output-file=requirements-dev.lock.txt requirements-dev.txt
#
# WARNING: these versions require Python >= 3.10 (black, pylint, pytest and
# their dependencies dropped 3.9). The CI test matrix still covers 3.9, so it
# installs the unpinned requirements-dev.txt instead of this file.

# direct
black==26.3.0
pylint==4.0.5
pytest==9.0.2
responses==0.26.0

# transitive
astroid==4.0.4
certifi==2025.1.31
charset-normalizer==3.4.1
click==8.1.8
dill==0.4.1
idna==3.10
iniconfig==2.3.0
isort==8.0.1
mccabe==0.7.0
mypy_extensions==1.1.0
packaging==26.0
pathspec==1.0.4
platformdirs==4.9.4
pluggy==1.6.0
pygments==2.19.2
pytokens==0.4.1
pyyaml==6.0.2
requests==2.32.3
tomlkit==0.14.0
typing_extensions==4.15.0
urllib3==2.3.0

# conditional, for Python < 3.11 (pytest, pylint, black); absent from the
# dev environment (3.12), versions taken from PyPI
exceptiongroup==1.3.1 ; python_version < "3.11"
tomli==2.4.1 ; python_version < "3.11"
